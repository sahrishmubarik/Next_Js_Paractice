import "client-only";
export const clientSideFunction=()=>{
    console.log(`client side component function,
        ues window object and other client side only features`);
        return("Client side function executed");
}